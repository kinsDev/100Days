/**
 * return-type function-name(arguments)
 * {
 *    declarations
 *    statements
 * }
 *
 * Return type of a function is the type of value the function returns.
 * functions cannot return arrays
 * specifying that the return-type is void indicates that the function cannot return a value
 * do not omitt the return type
 */


/*
double average(double a, double b)
{
    double sum;    declaration - we are declaring sum to be of type double

    sum = a + b;     statement;
    return sum / 2;  statement;
}
*/