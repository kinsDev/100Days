/**
 * If i and j are positive integers, does (-i) /j always have the same value as - (i/j ) ? Jus¬
tify your answer.
*/


#include <stdio.h>
int main(void)
{
    int i = 2, j = 4;
    printf("%d\n", (-(i))/j);
    return 0;
}